import type { {{pascalCase name}}Block } from '@/services/directus';
import BlockContainer from '@components/BlockContainer';

export const {{pascalCase name}} = ({}: {{pascalCase name}}Block["item"]) => {
  return <BlockContainer></BlockContainer>;
};